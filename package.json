{
  "name": "discord-rankup",
  "version": "0.9.1",
  "description": "A nice greeter",
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "files": [
    "lib/**/*"
  ],
  "scripts": {
    "build": "tsc",
    "test": "jest --config jestconfig.json",
    "compodoc": "npx compodoc -p tsconfig.doc.json -t laravel -d ./docs",
    "compodoc:serve": "npx compodoc -p tsconfig.doc.json --theme laravel -s",
    "lint": "eslint . --ext .ts",
    "lint:fix": "eslint . --ext .ts --fix",
    "format": "prettier --write \"src/**/*.ts\"",
    "prepare": "npm run build",
    "preversion": "npm run lint && npm run build && npm run test",
    "version": "npm run format && git add .",
    "postversion": "git push && git push --tags"
  },
  "author": "x404dev",
  "license": "ISC",
  "keywords": [
    "discord",
    "discord.js",
    "ranks",
    "levels",
    "xp"
  ],
  "devDependencies": {
    "@types/jest": "^29.4.1",
    "@typescript-eslint/eslint-plugin": "^5.55.0",
    "@typescript-eslint/parser": "^5.55.0",
    "dotenv": "^16.0.3",
    "eslint": "^8.36.0",
    "eslint-config-prettier": "^8.7.0",
    "eslint-plugin-prettier": "^4.2.1",
    "jest": "^29.5.0",
    "prettier": "^2.8.4",
    "ts-jest": "^29.0.5",
    "typescript": "^4.9.5"
  },
  "dependencies": {
    "discord.js": "^14.8.0",
    "mongoose": "^7.0.1"
  }
}
